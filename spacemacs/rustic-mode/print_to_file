# -*- mode: snippet -*-
# name: print_to_file
# key: pf
# --

let mut file = std::fs::OpenOptions::new()
.read(true)
.append(true)
.write(true)
.create(true)
.open("/home/sam/output").unwrap();
use std::io::Write;
write!(&mut file, "input: {:?}",).unwrap();

